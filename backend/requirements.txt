# Kairos Backend - Python Dependencies
# Install with: pip install -r requirements.txt

# Web framework
flask>=3.0.0
flask-cors>=4.0.0

# PDF processing
pypdf>=4.0.0
reportlab>=4.0.0
Pillow>=10.0.0

# PDF to image (also requires poppler installed on the system)
# Windows: https://github.com/oschwartz10612/poppler-windows/releases
# macOS:   brew install poppler
# Linux:   apt install poppler-utils
pdf2image>=1.16.0

# Microsoft Office formats
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=0.6.23

# Word/PPT to PDF (optional - Windows with MS Word installed)
# docx2pdf>=0.1.8

# Utility
Werkzeug>=3.0.0

# AI extraction (timetable parsing)
anthropic>=0.40.0

# Production server (Render)
gunicorn>=21.2.0
